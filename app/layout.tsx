import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navigation from "@/components/Navigation";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "CRUXSTOR | Advanced Screening & Sand Processing Systems",
  description: "CRUXSTOR - Leading manufacturer of advanced screening and sand processing systems. Quality industrial equipment for efficient material processing and separation.",
  keywords: "CRUXSTOR, screening systems, sand processing, industrial equipment, material processing, separation systems",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}

